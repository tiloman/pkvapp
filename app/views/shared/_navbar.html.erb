<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <%= link_to root_path, class:"navbar-brand" do %>
  PKV-Organizer<% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to "Personen", people_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Vorgänge", operations_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>

  </div>

  <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <%= link_to "Login", new_user_session_url, class: 'nav-item nav-link' unless user_signed_in? %>
        <%= link_to "Registrieren", new_user_registration_url, class: 'nav-item nav-link' unless user_signed_in? %>  
        <%= link_to current_user.email, edit_user_registration_path(current_user), class: 'nav-item nav-link' if user_signed_in? %>  
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'nav-item nav-link' if user_signed_in? %>  
      </ul>
    </div>
</nav>