<h1>Integrationen</h1>
<div class="card integration_card todoist">
  <div class="card-body">
    <div class="d-flex align-items-center mb-3">
      <%= image_tag 'todoist.png' %>
      <h3 class="p-0 ms-3 mb-0">Todoist</h3>
    </div>
    <% if @todoist_integration %>
      <p class="text-muted">API Token: <%= @todoist_integration.token %></p>
      <%= link_to "Editieren", edit_todoist_integration_path(@todoist_integration.id), class: 'btn btn-secondary' %>
    <% else %>
      <%= link_to "Neue Todoist integration", new_todoist_integration_path, class: 'btn btn-secondary' %>
    <% end %>
  </div>
</div>
